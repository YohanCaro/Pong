package co.edu.uptc.exceptions;

public class ComExceptions extends Exception {

	public ComExceptions(String message) {
		super(message);
	}
	
	/*
	 * Crear una clase persona con id, nombre, apellido, numro y tipo de documento que mediante el controllador 
	 * invocandolo dentro de un main
	 * pueda persistir un listado utilizando la clase genericDAO y enum y tipo a almacenar
	 * como salida 
	 * Contro listado de personas
	 * Modelo: unica persona
	 * HashMap por cada enum un dao
	 * Metodo persirtir que requiere el enum y by el listado de personas
	 * ----------------------------------------------------------------------------------------------------------
	 * Reportes
	 * JasperReport->Tibco Studio
	 */
}